<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cachedemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

      <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
          <activity android:name=".DBitmapActivity" />
        <activity android:name=".BJsonObjectActivity" />
        <activity android:name=".HObjectActivity" />
        <activity android:name=".AStringActivity" />
        <activity android:name=".EMediaActivity" />
        <activity
            android:name=".FDrawableActivity">
        </activity>
        <activity
            android:name=".CJsonArrayActivity">
        </activity>
    </application>

</manifest>
